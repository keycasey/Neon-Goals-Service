[project]
name = "scraper-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "requests>=2.32.3",
    "pydantic>=2.9.2",
]

[project.scripts]
start = "uvicorn main:app --host 0.0.0.0 --port 5000"
